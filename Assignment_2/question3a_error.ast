load system io.

let num = 10.
let str = "20".

io @ println(num + str).

-- creates an error due to it adding an integer and a string
-- I would fix this by converting the string to an integer by doing:
-- io @ println(num + tointeger(str)).